#!/usr/bin/env sh
set -e

REPO_ROOT="$(git rev-parse --show-toplevel)"
powershell.exe -NoProfile -ExecutionPolicy Bypass -File "$REPO_ROOT/scripts/update-task-tracker.ps1" -RepoRoot "$REPO_ROOT"

git add "$REPO_ROOT/TASK_TRACKER.md"
